1. Copy the datasets and the scripts directory from ther `lesson-3` to the `lesson-2/runzi`
2. Optionally tailor the good and bad datasets
3. Optionally install the runzi's `eval_demo_app` as an editable install with `uv pip install -e .`
4. Run the generate_logs script: `python scripts/generate_logs.py`
